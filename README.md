# 電子看板カメラ v65.24

## v65.1 内部整理
- `main` の v64 を維持したままレビュー枝で整理開始
- 巨大な `index.html` からCSSと主要JavaScriptを外出し
- IndexedDB保存失敗時に撮影・看板修正・写真取込を成功扱いしないよう修正
- Service Workerのキャッシュ対象へ新しいCSS/JSを追加
- UI、看板、命名規則、操作仕様は変更しない

## 主な変更
- 通常表示写真をダブルタップ／ダブルクリックで拡大表示
- 看板編集時の「採取」を「サンプリング」へ変更
- 調査は青、サンプリングは緑で表示
- 「看板編集」の横に現在の看板種類を表示
- 看板の行高さを調整
  - 件名 52
  - 採取場所 40
  - 採取箇所／部屋No. 34
  - 試料No. 34
  - 区分 30
  - 日付 40
- 試料No.の箇所番号は `1` を下限とし、`1 ↔ 1-① ↔ 1-②` のように増減
- サンプリング時の採取箇所を矢印で補助増減
- `101`、`第2ビル 101`、英字、方角などを簡易判定
- `1-2階` などの範囲はセットで `1-2 → 2-3 → 3-4` と増減
- 判定できない表記は看板編集から手入力
- 編集完了ボタン、設定ボタン再押下で閉じる動作を維持


## v61 統合修正版
- v56で確認済みの枝番採番、サムネスワイプ、区分固定、看板編集反映を維持
- 看板編集の各文字サイズに項目別リセットを追加（18/17/24/24/24）
- 案件選択を左上、枚数表示を小型化し、選択画像を上寄せ
- 選択画像は表示枠内で縦横比維持・全体表示
- サムネイルは左右スクロールのみ。上下スクロールとページ横移動を抑止


## v62 追加機能
- 起動時に「カメラを起動」「写真から看板を付ける」を選択
- カメラロール等から複数写真を選択可能
- 既存の看板編集UIを使って1枚ずつ連続編集
- 前の写真の看板内容を次の写真へ引き継ぎ
- 編集進捗を「1 / N」で表示
- 編集中の元写真をIndexedDBへ一時保持し、再起動時に再開/破棄を選択
- 全件完了後は一時元画像を破棄し、看板付き写真だけ写真一覧へ追加
- 読み込み写真も既存の命名ルールでファイル名を生成
- EXIF向きを考慮して画像を正規化
- 読み込み編集時に4箇所の看板位置と既存サイズ変更を利用可能

## v64 修正

- 看板編集プレビューの区分表示が、調査 / サンプリング切替に即時追従するよう修正
  - 調査: 目視 / 施工前 / 施工中 / 施工後
  - サンプリング: 施工前 / 施工中 / 施工後
- 編集プレビューの背景用画像要素を完全非表示化し、表示をCanvas一本に統一
- 写真 + 看板の編集プレビューと保存時の描画ロジックは従来どおり共通化

## v63 修正
- 写真読み込み時の看板編集を「写真 + 看板」の実画像プレビューへ変更
- 調査 / サンプリング切替を編集プレビューへ即時反映
- 看板位置4か所・看板サイズ変更をプレビューへ即時反映
- 読み込み写真の保存時もプレビューと同じ位置・サイズ比率で看板を合成

## v65.2 内部整理
- 旧 v49 `script.js` を削除
- 重複していた `loadImage()` の旧定義を削除
- HTML/JSから参照されない旧関数10件を削除
- 現行挙動は変更せず、残骸整理のみ実施

## v65.3 内部整理
- HTMLに存在しない旧DOM参照5件を削除
- 対応する常時無効だった表示更新処理を削除
- UI・操作・保存仕様は変更なし

## v65.4 保存層分離
- IndexedDBの写真/読込セッション操作を `js/photo-store.js` へ分離
- `app.js` はUI・撮影フロー側の責務へ寄せる
- DB名/バージョン/ストア名/接続状態を保存層へ集約
- 既存の呼び出し名は薄い別名で維持し、挙動変更を抑制

## v65.5 写真ビュー分離
- 写真案件管理・プレビュー・サムネイル/一覧・選択・並び順を `js/photo-viewer.js` へ分離
- 保存/共有・削除・写真拡大・看板修正導線も写真ビュー側へ集約
- 撮影処理・看板描画本体は `app.js` に維持
- UIと操作仕様は変更なし

## v65.6 写真取込分離
- 既存写真の複数選択・取込開始を `js/photo-import.js` へ分離
- IndexedDB一時セッションの再開/破棄・進捗管理を同モジュールへ集約
- 1枚ずつの看板編集・保存・次写真への引継ぎを取込フローとして分離
- カメラ撮影と看板描画本体は `app.js` に維持
- UIと操作仕様は変更なし

## v65.7 カメラ撮影分離
- カメラ起動/停止/プレビュー復帰を `js/camera.js` へ分離
- 断面モード、撮影、撮影直後確認、静止表示、画像生成を同モジュールへ集約
- 看板描画ルール本体 `drawBoardOnCanvas()` は `app.js` に維持
- UI・撮影仕様・ファイル名規則は変更なし

## v65.8 看板処理分離
- 看板初期化、入力、調査/サンプリング切替、採取箇所操作を `js/board.js` へ分離
- 看板位置/サイズ、編集、Undo/Redo、Canvas描画を同モジュールへ集約
- 共有状態は `app.js` に残し、既存モジュールとの挙動を維持
- UI・看板デザイン・撮影仕様は変更なし

## v65.9 設定処理分離
- 画質設定、看板文字サイズ設定を `js/settings.js` へ分離
- 設定画面の開閉、バージョン表示、更新確認/再読込を同モジュールへ集約
- 初期設定読込のため `settings.js` を `app.js` より先に読込
- UI・設定値・更新仕様は変更なし

## v65.10 共通写真処理整理
- 写真件数、区分、試料No.解析、ファイル名生成、保存写真読込を `js/photo-utils.js` へ分離
- IndexedDBの薄い中継関数を廃止し、各モジュールから `PhotoStore` を直接利用
- `app.js` は起動・共有状態・共通UI中心へ縮小
- UI・ファイル名規則・保存仕様は変更なし

## v65.11 引継ぎコメント整備
- 全JSモジュールへ責務と保守上の注意を日本語で追加
- 主要関数へ目的・理由が分かるコメントを追加
- `docs/maintenance-guide.md` を追加
- 機能・UI・データ仕様は変更なし

## v65.12 残構造監査
- 機能追加なしで `app.js` の残構造と各モジュール依存を監査
- 初期化インデント/JSDoc整形、`app.js` の責務区画を整理
- `docs/v65.12-structure-audit.md` に共有状態・定数・DOM参照の使用先を記録
- `samplingLocationInput` は外部参照があるため残し、誤削除を回避
- 共有状態は無理に移動せず、次段階の安全な整理候補として明示

## v65.13 モジュール所有整理
- 機能追加なし。単一所有のDOM参照・定数・実行状態を担当モジュールへ移動。
- 共有状態、保存方式、OneDrive、UI仕様は変更しない。


## v65.14 共有状態整理
- 複数モジュールが直接参照する共有定数・共有DOM・共有実行状態を `js/shared-state.js` へ集約
- 既存の変数名とclassic scriptの参照方法は維持し、機能側の呼び出し変更を回避
- `app.js` は初期化・全体イベント・app専用UIへ責務を縮小
- IndexedDB、写真形式、命名規則、UI、OneDrive連携仕様は変更なし


## v65.15 CSS監査・整理
- 見た目を変えずにCSSの後勝ち・重複構造を監査
- 成立しない `@media (orientation: portrait) and (max-width: 0px)` を削除
- 分割されていた `#boardEditHost` の `position: relative` を現行ブロックへ統合
- 看板編集のv52系以降の上書きは意図的な最終値が多いため、未確認の削除は行わない
- UI、看板寸法、IndexedDB、保存形式、OneDrive仕様は変更なし


## v65.16 PWA責務分離
- `js/pwa-controller.js` を追加し、PWA更新・Service Worker登録・iOS復帰制御を集約
- `settings.js` は画質・文字サイズ・設定画面だけに縮小
- `app.js` からPWA復帰イベントを除去し、機能初期化の司令塔へ整理
- `index.html` のinline Service Worker登録を廃止
- 圏外時の更新確認失敗は起動を止めない方針をコメントで明文化
- IndexedDB、写真形式、撮影、看板、ファイル名、OneDrive仕様は変更なし


## v65.17 ローカルアルバム責務分離
- `js/photo-album.js` を追加
- 案件別整理、並び替え、サムネ/一覧、選択/全選択、削除、撮影不足確認をviewerから移動
- `photo-viewer.js` は1枚表示、前後移動、拡大、看板修正、共有/外部保存へ縮小
- 写真本体の正本は引き続き `PhotoStore` / IndexedDB
- DB構造、Base64保存形式、撮影処理、ファイル名規則は変更なし


## v65.18 永続ストレージ・圏外起動強化
- `storage-manager.js` を追加し、利用可能端末でpersistent storageを要求
- 設定画面に永続化状態と推定ストレージ使用量を表示
- Service Workerのapp shellキャッシュを必須ファイル全成功方式へ変更
- 圏外navigation時はquery有無に依存せず `index.html` / `./` へフォールバック
- app shell静的ファイルはcache-firstとして圏外起動を優先
- 写真正本は引き続きPhotoStore / IndexedDB。DB schema・Base64形式は変更なし


## v65.19 端末保存診断・read-back確認
- PhotoStoreへ `getPhoto(photoId)` を追加
- 保存成功判定をtransaction完了だけでなく同IDのread-back実在確認まで強化
- `dataUrl` / `baseDataUrl` の存在も保存直後に確認
- 保存失敗時にerror.name / error.message / 写真概算サイズを撮影画面へ表示
- IndexedDB schema/version、Base64保存形式、撮影画質は変更なし
- 圏外起動修正は保存原因特定後へ延期


## v65.20 写真容量・推定撮影可能枚数
- IndexedDB内写真の `dataUrl + baseDataUrl` をアプリ自身で集計し、写真枚数と写真データ容量を表示
- `navigator.storage.estimate()` は「PWA全体の推定値」と明示して分離
- 推定空き容量を表示
- 現在の写真1枚あたり平均容量から推定撮影可能枚数を算出
- 写真0枚時は「1枚撮影後に算出」と表示
- 設定を開くたびに最新値を再集計
- 起動画面へ「⚙ 設定・更新」を常設し、写真0枚でも更新可能にした
- IndexedDB schema/version、Base64保存形式は変更なし


## v65.21 撮影セッション基盤
- 現段階の仮案件IDとして `yymmdd_枝番` を採用
- 同日2案件目以降は `_02`, `_03` と明示的に新規セッション開始
- 端末名をローカル保持し、保存先予定名を `端末名_yymmdd_枝番` で生成
- 件名は識別キーに使わない
- 撮影写真へ `caseId / caseDate / caseBranch / deviceName / oneDriveFolderName` を固定保存
- OneDrive用の `uploadStatus / uploadedAt / oneDriveItemId` を先行追加（送信自体は未実装）
- 日付が変わった場合は当日 `_01` セッションを自動開始
- DB schema/versionは変更なし


## v65.22 案件別アルバム / トップ導線
- 新規写真は `caseId`（現状 `yymmdd_枝番`）で案件グループを固定
- アルバム表示名は `案件番号_件名`
- 件名は識別キーにせず、その案件で最も新しい写真の看板件名を表示
- v65.20以前のcaseIdなし写真は従来どおり件名単位で表示する互換処理
- 写真読み込み機能で作る写真にも現在のcaseId/OneDrive予定情報を保存
- 写真アルバム上部に「トップ」ボタンを追加
- 撮影画面にはトップボタンを追加しない
- トップへ戻る時はカメラストリームを停止して起動画面へ戻る
- DB schema/versionは変更なし


## v65.23 トップ画面再構成
- トップを「選択中の案件 / 設定」「案件選択 / 新規案件」「カメラ起動 / 看板添付」の2列3段へ再構成
- 縦横どちらでも画面内に収まりやすいレスポンシブ寸法へ変更
- 選択中案件は仮案件ID + 現在の看板件名を表示
- 再起動・日付跨ぎで案件を自動切替せず、最後に選択した案件を保持
- 案件選択時はcaseIdをアクティブ化し、現段階では最新写真の件名を看板へ戻す
- 端末名変更はトップから設定画面へ移動


## v65.24 保存判定安定化
- 既存写真への看板添付は元画像(baseDataUrl)を保持しない仕様のため、完成画像のread-backを必須・元画像は保存対象に存在する場合のみ確認
- 看板修正時にPhotoStore.savePhoto()の構造化結果 `ok` を確認
- 看板修正保存失敗時はメモリ上の写真情報を旧値へ戻す
- 写真選択/全選択の保存結果を確認し、失敗時はメモリ状態を戻す
- 外部保存済みマークの保存結果を確認し、失敗時は保存済み表示だけ残さない
- DB schema/versionは2のまま変更なし
